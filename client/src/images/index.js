import clearDay from './clear-day.png';

const selection = (name) => {
    switch(name){
        case 'clear-day':
            return clearDay;
        default:
            return clearDay;
    }
}

export default selection;